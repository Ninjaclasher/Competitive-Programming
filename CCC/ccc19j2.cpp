#include <bits/stdc++.h>

using namespace std;

int main()
{
    int l;
    scanf("%i", &l);
    while (l--)
    {
        int t;
        char c;
        scanf("%i %c", &t, &c);
        while (t--)
            printf("%c", c);
        printf("\n");
    }
}
