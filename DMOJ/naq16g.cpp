#include <bits/stdc++.h>

using namespace std;

int main() {

    string in;
    cin>>in;
    double a = 0, b = in.length(), i = 0;
    while (a < b) a += log10(++i);
    {}
    if(in == "1")
    {
        cout<<"1";
    }else if(in == "2")
    {
        cout<<"2";
    }else
    {
        cout<<--i;
    }
}
