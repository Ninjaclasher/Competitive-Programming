#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    scanf("%i", &n);
    while (n--)
    {
        long long a, b;
        scanf("%lli%lli", &a, &b);
        printf("%lli\n", a-b);
    }
}
